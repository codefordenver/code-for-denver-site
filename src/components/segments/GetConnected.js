import React from 'react';
import MarkdownRenderer from '../MarkdownRenderer';

const GetConnected = () => (

  <section className="standard get_connected">
    <MarkdownRenderer fileName="get_connected" />
  </section>

);

export default GetConnected;
