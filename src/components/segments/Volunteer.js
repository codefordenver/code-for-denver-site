import React from 'react';
import MarkdownRenderer from '../MarkdownRenderer';

const Volunteer = () => (

  <section className="standard volunteer">
    <MarkdownRenderer fileName="volunteer" />
  </section>

);

export default Volunteer;
