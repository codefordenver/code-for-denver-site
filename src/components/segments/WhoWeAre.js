import React from 'react';
import MarkdownRenderer from '../MarkdownRenderer';

const WhoWeAre = () => (

  <section className="standard who_we_are">
    <MarkdownRenderer fileName="who_we_are" />
  </section>

);

export default WhoWeAre;
